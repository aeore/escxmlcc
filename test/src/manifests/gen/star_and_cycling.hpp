#ifndef AUTOFSM_STAR_AND_CYCLING_HPP
#define AUTOFSM_STAR_AND_CYCLING_HPP

/*
 *                                     escxmlcc product
 *                                           fsm
 *
 * @ingroup Autogenerated::FSM
 * @author  escxmlcc
 * 
 * @brief   This program was generated by escxmlcc fsm compiler written by Yuriy Gurin. This is
 *          an extended version of original scxmlcc compiler written by Jan Pedersen. For more
 *          information see project website http://escxmlcc.sourceforge.net
 */

// --------------------------------------------------------------------------
// Includes
// --------------------------------------------------------------------------

#include <typeinfo>
#include <memory>

// --------------------------------------------------------------------------
// User space
// --------------------------------------------------------------------------

/* no user data */

// --------------------------------------------------------------------------
// Code definition
// --------------------------------------------------------------------------

class IStar_and_cyclingActionHandler;
class CStar_and_cyclingFSM
{
public:
	struct data_model {
		friend class CStar_and_cyclingFSM;
		data_model( void ) { /* nop */ }
	private:
		IStar_and_cyclingActionHandler* actionHandler;
	} model;

public:
	class state
	{
	public:
		virtual state* event_e( CStar_and_cyclingFSM& ) { return 0; }
		virtual state* event_q( CStar_and_cyclingFSM& ) { return 0; }
		virtual state* event_r( CStar_and_cyclingFSM& ) { return 0; }
		virtual state* event_w( CStar_and_cyclingFSM& ) { return 0; }
		virtual state* event_z( CStar_and_cyclingFSM& ) { return 0; }
		virtual state* unconditional( CStar_and_cyclingFSM& ) { return 0; }
		virtual state* initial( CStar_and_cyclingFSM& ) { return 0; }

		template<class T> void enter( data_model&, ... ) {}
		template<class T> void exit( data_model&, ... ) {}
		virtual void exit( data_model&, const std::type_info& ) {}
		virtual ~state( void ) {}
	};

	state *cur_state;
	typedef state* ( state::*event )( CStar_and_cyclingFSM& );

	template<class C> class state_actions
	{
	protected:
		void inline enter( data_model& ) { /* default enter action */ }
		void inline exit( data_model& ) { /* default exit action */ }
	};

	template<class C, class P> class composite: public P, public state_actions<C>
	{
		virtual state* initial( CStar_and_cyclingFSM& ) { return 0; }
	public:
		// LCA calculation
		template<class T> void enter( data_model&, composite* ) {}
		template<class T> void enter( data_model &m, ... ) { P::template enter<T>( m, (T*)0 ); state_actions<C>::enter( m ); }
		template<class T> void exit( data_model&, composite* ) {}
		template<class T> void exit( data_model &m, ... ) { state_actions<C>::exit( m ); P::template exit<T>( m, (T*)0 ); }
		virtual void exit( data_model &m, const std::type_info &sti ) { if ( typeid(C) == sti ) return; state_actions<C>::exit( m ); P::exit( m, sti ); }
	};

	class no_state { /* dup */ };
	enum transition_type { external, internal };

	template<int index, event E, class S, class D0 = no_state> class transition_actions
	{
	protected:
		void enter(data_model&) { /* default enter action */ }
		bool condition(data_model&) { return true; } /* default condition action */
	};

	// external transition
	template<int index, event E, class S, class D = no_state, transition_type T = external> class transition: public transition_actions<index, E, S, D>
	{
	public:
		state* operator()( S *s, D &d, CStar_and_cyclingFSM &sc )
		{
			if( !transition_actions<index, E, S, D>::condition(sc.model) ) return 0;
			s->exit( sc.model, typeid(S) );
			s->template exit<D>( sc.model );
			transition_actions<index, E, S, D>::enter( sc.model );
			d.template enter<S>( sc.model );
			return &d;
		}
	};

	// internal transition
	template<int index, event E, class S, class D> class transition<index, E, S, D, internal>: public transition_actions<index, E, S, D>
	{
	public:
		state* operator()( S *s, D &d, CStar_and_cyclingFSM &sc )
		{
			if ( !transition_actions<index, E, S, D>::condition(sc.model) ) return 0;
			s->exit( sc.model, typeid(S) );
			s->template exit<D>( sc.model, (D*)0 );
			transition_actions<index, E, S, D>::enter( sc.model );
			d.template enter<S>( sc.model, (S*)0 );
			return &d;
		}
	};

	// transition with no target
	template<int index, event E, class S> class transition<index, E, S, no_state>: public transition_actions<index, E, S, no_state>
	{
	public:
		S* operator()( S *s, CStar_and_cyclingFSM &sc )
		{
			if ( !transition_actions<index, E, S, no_state>::condition(sc.model) ) return 0;
			transition_actions<index, E, S, no_state>::enter( sc.model );
			return s;
		}
	};

private:
	// --------------------------------------------------------------------------
	bool dispatch_event( event e )
	// --------------------------------------------------------------------------
	{
		state *next_state;
		if ( (next_state = (cur_state->*e)(*this)) ) cur_state = next_state;
		return next_state;
	}

public:
	// --------------------------------------------------------------------------
	void dispatch( event e )
	// --------------------------------------------------------------------------
	{
		bool cont = dispatch_event( e );
		while ( cont ) {
			if ( (cont = dispatch_event(&state::initial)) );
			else if ( (cont = dispatch_event(&state::unconditional)) );
			else break;
		}
	}

	// --------------------------------------------------------------------------
	CStar_and_cyclingFSM( IStar_and_cyclingActionHandler* pActionHandler ) : cur_state( &m_scxml )
	// --------------------------------------------------------------------------
	{
		model.actionHandler = pActionHandler;
	}

	// --------------------------------------------------------------------------
	void init( void )
	// --------------------------------------------------------------------------
	{
		dispatch( &state::initial );
	}

	class scxml : public composite<scxml, state>
	{
		state* initial( CStar_and_cyclingFSM&sc ) { return transition<0, &state::initial, scxml, state_init, internal>()( this, sc.m_state_init, sc ); }
	} m_scxml;

	class state_init: public composite<state_init, scxml>
	{
		state* event_e( CStar_and_cyclingFSM &sc ) {
			if ( true ) return transition<4, &state::event_e, state_init, state_3>()( this, sc.m_state_3, sc );
			else return 0;
		}
		state* event_q( CStar_and_cyclingFSM &sc ) {
			if ( true ) return transition<1, &state::event_q, state_init, state_1>()( this, sc.m_state_1, sc );
			else return 0;
		}
		state* event_r( CStar_and_cyclingFSM &sc ) {
			if ( true ) return transition<2, &state::event_r, state_init, state_4>()( this, sc.m_state_4, sc );
			else return 0;
		}
		state* event_w( CStar_and_cyclingFSM &sc ) {
			if ( true ) return transition<3, &state::event_w, state_init, state_2>()( this, sc.m_state_2, sc );
			else return 0;
		}
		virtual state* unconditional( CStar_and_cyclingFSM &sc ) { return 0; }
	} m_state_init;

	class state_1: public composite<state_1, scxml>
	{
		state* event_z( CStar_and_cyclingFSM &sc ) {
			if ( true ) return transition<5, &state::event_z, state_1, state_2>()( this, sc.m_state_2, sc );
			else return 0;
		}
		virtual state* unconditional( CStar_and_cyclingFSM &sc ) { return 0; }
	} m_state_1;

	class state_2: public composite<state_2, scxml>
	{
		state* event_z( CStar_and_cyclingFSM &sc ) {
			if ( true ) return transition<6, &state::event_z, state_2, state_3>()( this, sc.m_state_3, sc );
			else return 0;
		}
		virtual state* unconditional( CStar_and_cyclingFSM &sc ) { return 0; }
	} m_state_2;

	class state_3: public composite<state_3, scxml>
	{
		state* event_z( CStar_and_cyclingFSM &sc ) {
			if ( true ) return transition<7, &state::event_z, state_3, state_4>()( this, sc.m_state_4, sc );
			else return 0;
		}
		virtual state* unconditional( CStar_and_cyclingFSM &sc ) { return 0; }
	} m_state_3;

	class state_4: public composite<state_4, scxml>
	{
		state* event_z( CStar_and_cyclingFSM &sc ) {
			if ( true ) return transition<8, &state::event_z, state_4, state_1>()( this, sc.m_state_1, sc );
			else return 0;
		}
		virtual state* unconditional( CStar_and_cyclingFSM &sc ) { return 0; }
	} m_state_4;

};

class IStar_and_cyclingActionHandler
{
public:
	/* state handlers */
	virtual void stateInitEnter( CStar_and_cyclingFSM::data_model &m ) = 0;
	virtual void stateInitExit( CStar_and_cyclingFSM::data_model &m ) = 0;
	virtual void state1Enter( CStar_and_cyclingFSM::data_model &m ) = 0;
	virtual void state1Exit( CStar_and_cyclingFSM::data_model &m ) = 0;
	virtual void state2Enter( CStar_and_cyclingFSM::data_model &m ) = 0;
	virtual void state2Exit( CStar_and_cyclingFSM::data_model &m ) = 0;
	virtual void state3Enter( CStar_and_cyclingFSM::data_model &m ) = 0;
	virtual void state3Exit( CStar_and_cyclingFSM::data_model &m ) = 0;
	virtual void state4Enter( CStar_and_cyclingFSM::data_model &m ) = 0;
	virtual void state4Exit( CStar_and_cyclingFSM::data_model &m ) = 0;

	/* transition handlers */

protected:
	virtual ~IStar_and_cyclingActionHandler( void ) { /* nop */ }
};

class CStar_and_cyclingActionHandlerDefault: public IStar_and_cyclingActionHandler
{
public:
	/* state handlers */
	virtual void stateInitEnter( CStar_and_cyclingFSM::data_model &m ) { /* default */ }
	virtual void stateInitExit( CStar_and_cyclingFSM::data_model &m ) { /* default */ }
	virtual void state1Enter( CStar_and_cyclingFSM::data_model &m ) { /* default */ }
	virtual void state1Exit( CStar_and_cyclingFSM::data_model &m ) { /* default */ }
	virtual void state2Enter( CStar_and_cyclingFSM::data_model &m ) { /* default */ }
	virtual void state2Exit( CStar_and_cyclingFSM::data_model &m ) { /* default */ }
	virtual void state3Enter( CStar_and_cyclingFSM::data_model &m ) { /* default */ }
	virtual void state3Exit( CStar_and_cyclingFSM::data_model &m ) { /* default */ }
	virtual void state4Enter( CStar_and_cyclingFSM::data_model &m ) { /* default */ }
	virtual void state4Exit( CStar_and_cyclingFSM::data_model &m ) { /* default */ }

	/* transition handlers */

protected:
	virtual ~CStar_and_cyclingActionHandlerDefault( void ) { /* nop */ }
};


// --------------------------------------------------------------------------
template<> inline void CStar_and_cyclingFSM::state_actions<CStar_and_cyclingFSM::state_init>::enter( CStar_and_cyclingFSM::data_model &m )
// --------------------------------------------------------------------------
{
	m.actionHandler->stateInitEnter( m );
}

// --------------------------------------------------------------------------
template<> inline void CStar_and_cyclingFSM::state_actions<CStar_and_cyclingFSM::state_init>::exit( CStar_and_cyclingFSM::data_model &m )
// --------------------------------------------------------------------------
{
	m.actionHandler->stateInitExit( m );
}

// --------------------------------------------------------------------------
template<> inline void CStar_and_cyclingFSM::state_actions<CStar_and_cyclingFSM::state_1>::enter( CStar_and_cyclingFSM::data_model &m )
// --------------------------------------------------------------------------
{
	m.actionHandler->state1Enter( m );
}

// --------------------------------------------------------------------------
template<> inline void CStar_and_cyclingFSM::state_actions<CStar_and_cyclingFSM::state_1>::exit( CStar_and_cyclingFSM::data_model &m )
// --------------------------------------------------------------------------
{
	m.actionHandler->state1Exit( m );
}

// --------------------------------------------------------------------------
template<> inline void CStar_and_cyclingFSM::state_actions<CStar_and_cyclingFSM::state_2>::enter( CStar_and_cyclingFSM::data_model &m )
// --------------------------------------------------------------------------
{
	m.actionHandler->state2Enter( m );
}

// --------------------------------------------------------------------------
template<> inline void CStar_and_cyclingFSM::state_actions<CStar_and_cyclingFSM::state_2>::exit( CStar_and_cyclingFSM::data_model &m )
// --------------------------------------------------------------------------
{
	m.actionHandler->state2Exit( m );
}

// --------------------------------------------------------------------------
template<> inline void CStar_and_cyclingFSM::state_actions<CStar_and_cyclingFSM::state_3>::enter( CStar_and_cyclingFSM::data_model &m )
// --------------------------------------------------------------------------
{
	m.actionHandler->state3Enter( m );
}

// --------------------------------------------------------------------------
template<> inline void CStar_and_cyclingFSM::state_actions<CStar_and_cyclingFSM::state_3>::exit( CStar_and_cyclingFSM::data_model &m )
// --------------------------------------------------------------------------
{
	m.actionHandler->state3Exit( m );
}

// --------------------------------------------------------------------------
template<> inline void CStar_and_cyclingFSM::state_actions<CStar_and_cyclingFSM::state_4>::enter( CStar_and_cyclingFSM::data_model &m )
// --------------------------------------------------------------------------
{
	m.actionHandler->state4Enter( m );
}

// --------------------------------------------------------------------------
template<> inline void CStar_and_cyclingFSM::state_actions<CStar_and_cyclingFSM::state_4>::exit( CStar_and_cyclingFSM::data_model &m )
// --------------------------------------------------------------------------
{
	m.actionHandler->state4Exit( m );
}

/* ************************************************************************ */
#endif // AUTOFSM_STAR_AND_CYCLING_HPP
