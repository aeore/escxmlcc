#ifndef AUTOFSM_SEMIEVENT_AND_CONDITIONS_HPP
#define AUTOFSM_SEMIEVENT_AND_CONDITIONS_HPP

/*
 *                                     escxmlcc product
 *                                           fsm
 *
 * @ingroup Autogenerated::FSM
 * @author  escxmlcc
 * 
 * @brief   This program was generated by escxmlcc fsm compiler written by Yuriy Gurin. This is
 *          an extended version of original scxmlcc compiler written by Jan Pedersen. For more
 *          information see project website http://escxmlcc.sourceforge.net
 */

// --------------------------------------------------------------------------
// Includes
// --------------------------------------------------------------------------

#include <typeinfo>
#include <memory>
#include "Environment/Environment.hpp"

// --------------------------------------------------------------------------
// User space
// --------------------------------------------------------------------------

/* no user data */

// --------------------------------------------------------------------------
// Code definition
// --------------------------------------------------------------------------

class ISemievent_and_conditionsActionHandler;
class CSemievent_and_conditionsFSM
{
public:
	struct data_model {
		friend class CSemievent_and_conditionsFSM;
		data_model( void ): index() { /* nop */ }
		int index;
	private:
		ISemievent_and_conditionsActionHandler* actionHandler;
	} model;

public:
	// --------------------------------------------------------------------------
	void setIndex( const int& value )
	// --------------------------------------------------------------------------
	{
		model.index = value;
	}

public:
	class state
	{
	public:
		virtual state* trigger_q( CSemievent_and_conditionsFSM& ) { return 0; }
		virtual state* unconditional( CSemievent_and_conditionsFSM& ) { return 0; }
		virtual state* unconditional_async( CSemievent_and_conditionsFSM& ) { return 0; }
		virtual state* initial( CSemievent_and_conditionsFSM& ) { return 0; }

		template<class T> void enter( data_model&, ... ) {}
		template<class T> void exit( data_model&, ... ) {}
		virtual void exit( data_model&, const std::type_info& ) {}
		virtual ~state( void ) {}
	};

	state *cur_state;
	typedef state* ( state::*event )( CSemievent_and_conditionsFSM& );

private: 
	class CPAsyncEventTransitionData
	{
	public:
		// --------------------------------------------------------------------------
		CPAsyncEventTransitionData( CSemievent_and_conditionsFSM& fsm, const event trigger ): mFSM( fsm ), mTrigger( trigger ) { /* none */ }
		// --------------------------------------------------------------------------

		// --------------------------------------------------------------------------
		inline void dispatch( void ) { mFSM.dispatch( mTrigger ); }
		// --------------------------------------------------------------------------

	private:
		CSemievent_and_conditionsFSM& mFSM;
		event mTrigger;
	};

	DECLARE_EVENT( CPAsyncEvent, CPAsyncEventTransitionData, IPAsyncEventConsumer );

	class CPAsyncTriggerInvoker : public IPAsyncEventConsumer
	{
	public:
		// --------------------------------------------------------------------------
		CPAsyncTriggerInvoker( CSemievent_and_conditionsFSM& fsm ) : mFsm( fsm ) { /* none */ }
		// --------------------------------------------------------------------------

		// --------------------------------------------------------------------------
		inline state* makeAsyncCall( const event trigger ) {
		// --------------------------------------------------------------------------
			CPAsyncEvent* e = CPAsyncEvent::createEvent( CPAsyncEventTransitionData(mFsm, trigger) );
			e->setConsumer( this );
			e->send();
			return 0;
		}

		// --------------------------------------------------------------------------
		inline state* makeAsyncUCall( const event trigger ) {
		// --------------------------------------------------------------------------
			CPAsyncEvent* e = CPAsyncEvent::createEvent( CPAsyncEventTransitionData(mFsm, trigger) );
			e->setConsumer( this );
			e->send();
			return (state*)0xFFFFFF;
		}

	protected:
		// --------------------------------------------------------------------------
		inline virtual void processEvent( const CPAsyncEvent& event ) { event.getData().dispatch(); }
		// --------------------------------------------------------------------------

	private:
		CSemievent_and_conditionsFSM& mFsm;
	} asyncTriggerInvoker;

public:
	template<class C> class state_actions
	{
	protected:
		void inline enter( data_model& ) { /* default enter action */ }
		void inline exit( data_model& ) { /* default exit action */ }
	};

	template<class C, class P> class composite: public P, public state_actions<C>
	{
		virtual state* initial( CSemievent_and_conditionsFSM& ) { return 0; }
	public:
		// LCA calculation
		template<class T> void enter( data_model&, composite* ) {}
		template<class T> void enter( data_model &m, ... ) { P::template enter<T>( m, (T*)0 ); state_actions<C>::enter( m ); }
		template<class T> void exit( data_model&, composite* ) {}
		template<class T> void exit( data_model &m, ... ) { state_actions<C>::exit( m ); P::template exit<T>( m, (T*)0 ); }
		virtual void exit( data_model &m, const std::type_info &sti ) { if ( typeid(C) == sti ) return; state_actions<C>::exit( m ); P::exit( m, sti ); }
	};

	class no_state { /* dup */ };
	enum transition_type { external, internal };

	template<int index, event E, class S, class D0 = no_state> class transition_actions
	{
	protected:
		void enter(data_model&) { /* default enter action */ }
		bool condition(data_model&) { return true; } /* default condition action */
	};

	// external transition
	template<int index, event E, class S, class D = no_state, transition_type T = external> class transition: public transition_actions<index, E, S, D>
	{
	public:
		state* operator()( S *s, D &d, CSemievent_and_conditionsFSM &sc )
		{
			if( !transition_actions<index, E, S, D>::condition(sc.model) ) return 0;
			s->exit( sc.model, typeid(S) );
			s->template exit<D>( sc.model );
			transition_actions<index, E, S, D>::enter( sc.model );
			d.template enter<S>( sc.model );
			return &d;
		}
	};

	// internal transition
	template<int index, event E, class S, class D> class transition<index, E, S, D, internal>: public transition_actions<index, E, S, D>
	{
	public:
		state* operator()( S *s, D &d, CSemievent_and_conditionsFSM &sc )
		{
			if ( !transition_actions<index, E, S, D>::condition(sc.model) ) return 0;
			s->exit( sc.model, typeid(S) );
			s->template exit<D>( sc.model, (D*)0 );
			transition_actions<index, E, S, D>::enter( sc.model );
			d.template enter<S>( sc.model, (S*)0 );
			return &d;
		}
	};

	// transition with no target
	template<int index, event E, class S> class transition<index, E, S, no_state>: public transition_actions<index, E, S, no_state>
	{
	public:
		S* operator()( S *s, CSemievent_and_conditionsFSM &sc )
		{
			if ( !transition_actions<index, E, S, no_state>::condition(sc.model) ) return 0;
			transition_actions<index, E, S, no_state>::enter( sc.model );
			return s;
		}
	};

private:
	// --------------------------------------------------------------------------
	bool dispatch_event( event e )
	// --------------------------------------------------------------------------
	{
		state *next_state;
		if ( (next_state = (cur_state->*e)(*this)) ) cur_state = next_state;
		return next_state;
	}
	// --------------------------------------------------------------------------
	bool dispatch_uasync( event e )
	// --------------------------------------------------------------------------
	{
		return (cur_state->*e)(*this) == (state*)0xFFFFFF;
	}

public:
	// --------------------------------------------------------------------------
	void dispatch( event e )
	// --------------------------------------------------------------------------
	{
		bool cont = dispatch_event( e );
		while ( cont ) {
			if ( (cont = dispatch_event(&state::initial)) );
			else if ( dispatch_uasync(&state::unconditional_async) );
			else if ( cont = dispatch_event(&state::unconditional) );
			else break;
		}
	}

	// --------------------------------------------------------------------------
	CSemievent_and_conditionsFSM( ISemievent_and_conditionsActionHandler* pActionHandler ) : cur_state( &m_scxml ), asyncTriggerInvoker( *this )
	// --------------------------------------------------------------------------
	{
		model.actionHandler = pActionHandler;
	}

	// --------------------------------------------------------------------------
	void init( void )
	// --------------------------------------------------------------------------
	{
		dispatch( &state::initial );
	}

	class scxml : public composite<scxml, state>
	{
		// --------------------------------------------------------------------------
		state* initial( CSemievent_and_conditionsFSM&sc ) {
		// --------------------------------------------------------------------------
			return transition<0, &state::initial, scxml, state_init, internal>()( this, sc.m_state_init, sc ); }
		} m_scxml;

	class state_init: public composite<state_init, scxml>
	{
		// --------------------------------------------------------------------------
		inline state* trigger_q( CSemievent_and_conditionsFSM &sc ) {
		// --------------------------------------------------------------------------
			int& index = sc.model.index;
			if ( index < 10 ) return transition<1, &state::trigger_q, state_init, state_A>()( this, sc.m_state_A, sc );
			if ( index > 10 ) return transition<2, &state::trigger_q, state_init, state_B>()( this, sc.m_state_B, sc );
			if ( index == 10 ) return transition<3, &state::trigger_q, state_init, state_C>()( this, sc.m_state_C, sc );
			else return 0;
		}
		virtual state* unconditional( CSemievent_and_conditionsFSM &sc ) { return 0; }
	} m_state_init;

	class state_A: public composite<state_A, scxml>
	{
		// --------------------------------------------------------------------------
		inline virtual state* unconditional( CSemievent_and_conditionsFSM &sc ) {
		// --------------------------------------------------------------------------
			if ( true ) return transition<4, &state::unconditional, state_A, state_init>()( this, sc.m_state_init, sc );
			else return 0;
		}
	} m_state_A;

	class state_B: public composite<state_B, scxml>
	{
		// --------------------------------------------------------------------------
		inline virtual state* unconditional( CSemievent_and_conditionsFSM &sc ) {
		// --------------------------------------------------------------------------
			if ( true ) return transition<5, &state::unconditional, state_B, state_init>()( this, sc.m_state_init, sc );
			else return 0;
		}
	} m_state_B;

	class state_C: public composite<state_C, scxml>
	{
		// --------------------------------------------------------------------------
		inline virtual state* unconditional( CSemievent_and_conditionsFSM &sc ) {
		// --------------------------------------------------------------------------
			if ( true ) return transition<6, &state::unconditional, state_C, state_init>()( this, sc.m_state_init, sc );
			else return 0;
		}
	} m_state_C;

};

class ISemievent_and_conditionsActionHandler
{
public:
	/* state handlers */
	virtual void stateInitEnter( CSemievent_and_conditionsFSM::data_model &m ) = 0;
	virtual void stateInitExit( CSemievent_and_conditionsFSM::data_model &m ) = 0;
	virtual void stateAEnter( CSemievent_and_conditionsFSM::data_model &m ) = 0;
	virtual void stateAExit( CSemievent_and_conditionsFSM::data_model &m ) = 0;
	virtual void stateBEnter( CSemievent_and_conditionsFSM::data_model &m ) = 0;
	virtual void stateBExit( CSemievent_and_conditionsFSM::data_model &m ) = 0;
	virtual void stateCEnter( CSemievent_and_conditionsFSM::data_model &m ) = 0;
	virtual void stateCExit( CSemievent_and_conditionsFSM::data_model &m ) = 0;

	/* transition handlers */

protected:
	virtual ~ISemievent_and_conditionsActionHandler( void ) { /* nop */ }
};

class CSemievent_and_conditionsActionHandlerDefault: public ISemievent_and_conditionsActionHandler
{
public:
	/* state handlers */
	virtual void stateInitEnter( CSemievent_and_conditionsFSM::data_model &m ) { /* default */ }
	virtual void stateInitExit( CSemievent_and_conditionsFSM::data_model &m ) { /* default */ }
	virtual void stateAEnter( CSemievent_and_conditionsFSM::data_model &m ) { /* default */ }
	virtual void stateAExit( CSemievent_and_conditionsFSM::data_model &m ) { /* default */ }
	virtual void stateBEnter( CSemievent_and_conditionsFSM::data_model &m ) { /* default */ }
	virtual void stateBExit( CSemievent_and_conditionsFSM::data_model &m ) { /* default */ }
	virtual void stateCEnter( CSemievent_and_conditionsFSM::data_model &m ) { /* default */ }
	virtual void stateCExit( CSemievent_and_conditionsFSM::data_model &m ) { /* default */ }

	/* transition handlers */

protected:
	virtual ~CSemievent_and_conditionsActionHandlerDefault( void ) { /* nop */ }
};


// --------------------------------------------------------------------------
template<> inline void CSemievent_and_conditionsFSM::state_actions<CSemievent_and_conditionsFSM::state_init>::enter( CSemievent_and_conditionsFSM::data_model &m )
// --------------------------------------------------------------------------
{
	m.actionHandler->stateInitEnter( m );
}

// --------------------------------------------------------------------------
template<> inline void CSemievent_and_conditionsFSM::state_actions<CSemievent_and_conditionsFSM::state_init>::exit( CSemievent_and_conditionsFSM::data_model &m )
// --------------------------------------------------------------------------
{
	m.actionHandler->stateInitExit( m );
}

// --------------------------------------------------------------------------
template<> inline void CSemievent_and_conditionsFSM::state_actions<CSemievent_and_conditionsFSM::state_A>::enter( CSemievent_and_conditionsFSM::data_model &m )
// --------------------------------------------------------------------------
{
	m.actionHandler->stateAEnter( m );
}

// --------------------------------------------------------------------------
template<> inline void CSemievent_and_conditionsFSM::state_actions<CSemievent_and_conditionsFSM::state_A>::exit( CSemievent_and_conditionsFSM::data_model &m )
// --------------------------------------------------------------------------
{
	m.actionHandler->stateAExit( m );
}

// --------------------------------------------------------------------------
template<> inline void CSemievent_and_conditionsFSM::state_actions<CSemievent_and_conditionsFSM::state_B>::enter( CSemievent_and_conditionsFSM::data_model &m )
// --------------------------------------------------------------------------
{
	m.actionHandler->stateBEnter( m );
}

// --------------------------------------------------------------------------
template<> inline void CSemievent_and_conditionsFSM::state_actions<CSemievent_and_conditionsFSM::state_B>::exit( CSemievent_and_conditionsFSM::data_model &m )
// --------------------------------------------------------------------------
{
	m.actionHandler->stateBExit( m );
}

// --------------------------------------------------------------------------
template<> inline void CSemievent_and_conditionsFSM::state_actions<CSemievent_and_conditionsFSM::state_C>::enter( CSemievent_and_conditionsFSM::data_model &m )
// --------------------------------------------------------------------------
{
	m.actionHandler->stateCEnter( m );
}

// --------------------------------------------------------------------------
template<> inline void CSemievent_and_conditionsFSM::state_actions<CSemievent_and_conditionsFSM::state_C>::exit( CSemievent_and_conditionsFSM::data_model &m )
// --------------------------------------------------------------------------
{
	m.actionHandler->stateCExit( m );
}

/* ************************************************************************ */
#endif // AUTOFSM_SEMIEVENT_AND_CONDITIONS_HPP
